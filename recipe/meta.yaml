{% set name = "ipymesh" %}
{% set version = "0.1.6" %}
{% set build = 0 %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: de004bf930fe1469cd96aca66c38ef71487412b28788905a4d2c36b0edd0eb36

build:
  noarch: python
  number: {{ build }}
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python >=3.6.0
    - pip
    - nodejs
  run:
    - python >=3.6.0
    - ipywidgets >=7.0.0

test:
  imports:
    - {{ name }}

about:
  home: https://github.com/erdc/ipymesh
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: A Custom Mesh Widget Library
  doc_url: https://github.com/erdc/ipymesh
  dev_url: https://github.com/erdc/ipymesh

extra:
  recipe-maintainers:
    - davidbrochart
